# Тема: Анти-патерн "God Object" та принцип єдиної відповідальності (SRP)  

## Характеристики анти-патерну "God Object"

**God Object** — це клас, який бере на себе занадто багато обов'язків. Замість того, щоб бути частиною системи, він стає самою системою.

**Основні ознаки такого класу:**
1.  **Надмірна функціональність:** Клас намагається вирішувати всі задачі одночасно (бізнес-логіка, збереження в БД, логування, відправка повідомлень).
2.  **Величезний розмір:** Такі класи часто містять тисячі рядків коду.
3.  **Тісний зв'язок (Tight Coupling):** Оскільки вся логіка переплетена, зміна однієї частини (наприклад, формату логів) може зламати іншу частину (наприклад, розрахунок ціни).
4.  **Складність тестування:** Написати Unit-тести для такого класу майже неможливо, оскільки доводиться імітувати (мокати) занадто багато залежностей.

## Практичний приклад (Порушення SRP)

Для прикладу взяла клас `OrderManager`. 

**Проблема:** Клас має більше однієї причини для змін. Він залежить і від правил розрахунку знижок, і від файлової системи, і від поштового сервісу.

### Код (C#):

```csharp
using System;
using System.IO;

public class OrderManager
{
    // Метод робить все підряд: рахує, зберігає, пише листи
    public void ProcessOrder(string productName, int quantity, double price, string email)
    {
        // 1. Логіка розрахунку (Business Logic)
        double total = price * quantity;
        if (quantity > 10)
        {
            total *= 0.9; // Знижка 10%
        }
        Console.WriteLine($"Ціна розрахована: {total}");

        // 2. Логіка збереження (Data Access)
        try
        {
            File.AppendAllText("orders_db.txt", $"{DateTime.Now}: {productName}, {total}\n");
        }
        catch (Exception ex)
        {
            // 3. Логіка логування (Logging)
            Console.WriteLine("Помилка запису: " + ex.Message);
            return;
        }

        // 4. Логіка сповіщення (Notification)
        Console.WriteLine($"Відправка email на {email}: Ваше замовлення прийнято.");
    }
}

## Рефакторинг (Реалізація принципу SRP)

**Нова структура проекту:**
1.  **`PriceCalculator`** — відповідає виключно за математичні розрахунки та логіку знижок.
2.  **`OrderRepository`** — відповідає за збереження даних (абстракція бази даних або файлової системи).
3.  **`EmailNotifier`** — відповідає за комунікацію з користувачем (відправка листів).
4.  **`OrderService`** — сервіс-координатор, який керує процесом, об'єднуючи роботу інших компонентів.

### 💻 Оновлений код 

```csharp
using System;

// Розрахунок ціни
public class PriceCalculator
{
    public double CalculateTotal(double price, int quantity)
    {
        double total = price * quantity;
        // Логіка знижки
        return quantity > 10 ? total * 0.9 : total;
    }
}

// Робота з даними
public class OrderRepository
{
    public void Save(string orderInfo)
    {
        // Імітація збереження
        Console.WriteLine($"[DB]: Збережено -> {orderInfo}");
    }
}

// Сповіщення
public class EmailNotifier
{
    public void SendConfirmation(string email, string message)
    {
        // Імітація відправки
        Console.WriteLine($"[Email] to {email}: {message}");
    }
}

// Координатор
public class OrderService
{
    private readonly PriceCalculator _calculator;
    private readonly OrderRepository _repository;
    private readonly EmailNotifier _notifier;

    // Ініціалізуємо залежності в конструкторі
    public OrderService()
    {
        _calculator = new PriceCalculator();
        _repository = new OrderRepository();
        _notifier = new EmailNotifier();
    }

    public void ProcessOrder(string productName, int quantity, double price, string email)
    {
        // Розрахунок
        double total = _calculator.CalculateTotal(price, quantity);

        // Збереження
        _repository.Save($"Order: {productName}, Total: {total}");

        // Сповіщення
        _notifier.SendConfirmation(email, $"Замовлення на суму {total} підтверджено.");
    }
}